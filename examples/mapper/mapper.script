local mapper = require "in.mapper"
local actions = require "examples.mapper.actions"

function init(self)
	mapper.bind(mapper.KEY_LEFT, actions.LEFT, 1)
	mapper.bind(mapper.KEY_RIGHT, actions.RIGHT, 1)
	
	mapper.bind(mapper.KEY_A, actions.LEFT, 2)
	mapper.bind(mapper.KEY_D, actions.RIGHT, 2)
end
